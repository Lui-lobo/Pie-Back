import { ListaReservasEvento } from './lista-reservas-evento.model';

describe('ListaReservasEvento', () => {
  it('should create an instance', () => {
    expect(new ListaReservasEvento()).toBeTruthy();
  });
});
