import { TrocaFormularioLogin } from './troca-formulario-login.model';

describe('TrocaFormularioLogin', () => {
  it('should create an instance', () => {
    expect(new TrocaFormularioLogin()).toBeTruthy();
  });
});
